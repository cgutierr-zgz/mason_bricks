name: 42_project
description: A simple 42 project starter
repository: https://github.com/cgutierr-zgz/mason_bricks/tree/master/bricks/42_project

# The following defines the version and build number for your brick.
# A version number is three numbers separated by dots, like 1.2.34
# followed by an optional build number (separated by a +).
version: 0.1.0

# The following defines the environment for the current brick.
# It includes the version of mason that the brick requires.
environment:
  mason: ">=0.1.0-dev.26 <0.1.0"

# Variables specify dynamic values that your brick depends on.
# Zero or more variables can be specified for a given brick.
# Each variable has:
#  * a type (string, number, boolean, enum, or array)
#  * an optional short description
#  * an optional default value
#  * an optional list of default values (array only)
#  * an optional prompt phrase used when asking for the variable
#  * a list of values (enums only)
vars:
  project_name:
    type: string
    description: Project name
    default: fract-ol
    prompt: Enter the project name

  description:
    type: string
    description: Project description
    default: A simple Fractal Renderer
    prompt: Enter the project description

  makefile:
    type: boolean
    description: Create a Makefile for the project
    default: true
    prompt: Do you want a Makefile?
